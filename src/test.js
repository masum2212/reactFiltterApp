
  let value = data.filter((value) =>{
    if(state === ''){
      return '';
    }else{
      if(state <=3){
        return '';
      }
      else{
        data
        .filter((item) => item.title.toLowerCase().includes(state))
        return value
      }
    }
  }
  
  )